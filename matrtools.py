def add(a,b):
    c_row=[]
# add of matrix
    for i in range(len(a)):
        c_col=[]
        for j in range(len(a[0])):
            c_col.append(a[i][j]+b[i][j])

        c_row.append(ccol)
        return c_row